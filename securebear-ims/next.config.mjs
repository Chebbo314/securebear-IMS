/** @type {import('next').NextConfig} */
const nextConfig = {};

// next.config.mjs
export default {
    output: 'export', // Required for static export
    trailingSlash: true, // Optional: Adds a trailing slash to URLs (e.g. /about/ instead of /about)
  }
  
